<% if (comment.body && !comment.archived?) %> 
    <div class="p-3 my-2 me-auto bg-dark  w-100">
        <p>
            <strong><%= comment.commenter %></strong>
        </p>
        <p>
        <%= comment.body %>
        </p>
        <p>
            <%= link_to "Delete Comment", [comment.article, comment], data: {
                          turbo_method: :delete,
                          turbo_confirm: "Are you sure?"
                        } %>
          </p>  
    </div>
<% end %> 

